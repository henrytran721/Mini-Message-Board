extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/new') Add Message
  each message in messages
    h1
      = message.user
    p
      = message.text
    p
      = message.added
    
